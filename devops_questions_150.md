Вот подборка из 150 актуальных вопросов и ответов по Linux 🐧 для собеседования в DevOps, которые охватывают ключевые темы, с которыми вы столкнетесь в 2025 году.
### Управление процессами и производительностью 

**1. Что такое Load Average (средняя нагрузка)?** <br/>
```yaml
Load Average в Linux — это скользящее среднее значение количества процессов, находящихся в состояниях:
    R (Running) — исполняющиеся в момент измерения
    D (Uninterruptible sleep) — ожидающие завершения системного вызова (обычно I/O операций)
    Математически представляет собой экспоненциально взвешенное среднее за 1, 5 и 15 минут, 
    вычисляемое ядром через дискретные выборки состояния очереди выполнения.
```

**2. Что такое Zombie-процесс (процесс-зомби) в чём разница между процессами сиротками?** <br/>
```bash
    Процесс, который завершил выполнение (exit()), но его запись еще существует в таблице процессов, 
    так как родитель не прочитал его статус завершения через wait().

    Причина возникновения:
    - Процесс завершился (exit()), но родитель не вызвал wait()/waitpid()
    - PID и запись в таблице процессов сохраняются для возможности чтения кода завершения

    Процессы-сироты (orphans)
    Причина возникновения:
    - Родительский процесс завершился дочернего
    - Дочерний процесс переусыновляется init (PID 1)
    # Процессы с удалёнными файлами
    lsof +L1  # файлы с link count = 0
```

**3. Какой процесс имеет PID 1 и за что он отвечает?** <br/>
```
    Это процесс systemd (или init в старых системах). 
    Он является родителем всех процессов, отвечает за загрузку системы и управление службами.
```

**4. В чем разница между процессом и потоком?** <br/>
```
    Процесс – изолированный экземпляр программы с собственными ресурсами.
    Поток (Thread) – часть процесса, делит с ним память и ресурсы, но имеет собственный стек и счетчик команд.
```
**5. Как найти и завершить процесс?** <br/>
```bash
    Найти PID: pgrep <name> или `ps aux	grep <name>.
    Завершить: kill <PID>(посылает SIGTERM) или kill -9 <PID>` (SIGKILL, принудительное завершение).
```
**6. Что такое nohup и для чего он используется?**<br/>
```
    nohup позволяет команде продолжать работу после завершения терминала или сессии пользователя, игнорируя сигнал SIGHUP (Hangup).
```

### Файловые системы и диски 💾
**7. В чем разница между жесткой (hard link) и символьной (soft/symbolic link) ссылкой?** <br/>
```
    Жесткая – еще одно имя для тех же данных (inode). Нельзя ссылаться на каталоги или другие файловые системы.
    Символьная – файл-указатель на путь, как ярлык. Может ссылаться на что угодно.
```
**8. Что такое inode?** <br/>
```
    Index node
    Индексный дескриптор – структура данных,
    хранящая метаинформацию о файле (права, владелец, timestamps), кроме его имени и содержимого.
```
**9. Что такое LVM (Logical Volume Manager) и зачем он нужен?**
```
    Технология для абстракции дискового пространства. Позволяет объединять несколько дисков в одну группу томов (VG) и гибко создавать/изменять размер логических томов (LV) на лету.
```

**10. Что такое Swap Space (пространство подкачки)?**
```
    Область на диске, используемая ядром как "дополнительная RAM" когда физической памяти не хватает. Реализуется как отдельный раздел или файл.
```

**11. Как увеличить размер существующей файловой системы?**
```
    Зависит от конфигурации. Обычно: 1) Увеличить базовый диск/LVM-том. 2) 
    Использовать resize2fs (для ext*) или xfs_growfs (для XFS) для расширения ФС.
```
**12. Что такое Mutex, в чём разница с семафором?**
```
Это синхронизация потоков:
    Мьютекс (mutex) — примитив синхронизации, обеспечивающий взаимное исключение (mutual exclusion) через атомарные операции над состоянием блокировки. 
    Только поток, захвативший мьютекс, может его освободить.

    Семафор (semaphore) — обобщенный счетчик синхронизации, реализующий семафор Дейкстры с операциями P (wait) и V (signal). 
    Может использоваться для ограничения доступа к пулу ресурсов.
```
**13. Разница между Ctrl+C и Ctrl+Z в терминале?**
```
    Ctrl+C посылает сигнал SIGINT (завершение процесса). 
    Ctrl+Z посылает сигнал SIGTSTP (приостановка процесса и помещение его в фоновый режим).
```

###  Права доступа и безопасность 🔐

**14. Что означают права 755 на файл?**
```bash
    rwxr-xr-x. Владелец: читать, писать, выполнять. Группа: читать, выполнять. 
    Остальные: читать, выполнять. (Чтение=4, Запись=2, Выполнение=1).
```
**15. Зачем директории нужен право на выполнение (x) executable?**
```
    Право на выполнение (x) для каталога дает возможность войти в него 
    (сделать его текущим рабочим каталогом) и получить доступ к файлам внутри него.
```
**16. Что такое Атомарные операции, в чём различие с булевыми например?**
```yaml
    Атомарные операции: это операции, которые выполняются как единое целое, без возможности прерывания их выполнения другими потоками или процессами. 
    В контексте многопоточности атомарная операция либо полностью выполняется, либо не выполняется вовсе, 
    и в момент ее выполнения другие потоки не могут наблюдать промежуточное состояние.

    Булевы операции: это операции с булевыми (логическими) значениями (true/false), 
                    такие как AND, OR, NOT, XOR и т.д. Они не обязательно являются атомарными.
```
**17. Как организовать доступ по SSH без пароля?**
```bash
    Сгенерировать пару SSH-ключей на клиенте (ssh-keygen). Скопировать публичный ключ на сервер в ~/.ssh/authorized_keys (команда ssh-copy-id).
```
### Сети и управление пакетами 🌐

**18. Как проверить, открыт ли порт на удаленном хосте?**
```bash
Использовать
    telnet <host> <port>
    nc -zv <host> <port> или nmap -p <port> <host>.
    curl, wget --spider # не закачивает
```
**19. Как проверить сетевую связность?**
```bash
Команда ping <host> для проверки доступности и traceroute <host> для прослеживания маршрута.
```
**20. Что такое фаервол (firewall) в Linux?**
```
    Программа или компонент ядра, который фильтрует сетевой трафик (входящий, исходящий, транзитный) на основе заданных правил. 
    Современный стандарт – nftables, ранее iptables.
```
**21. Какой пакетный менеджер используется в Debian/Ubuntu и RedHat/CentOS?**
```
Debian/Ubuntu: apt или apt-get. RedHat/CentOS: yum (старые версии) или dnf (новые версии).
```

### Администрирование и устранение неисправностей ⚙️ 

**22. Ситуация: df -h показывает свободное место, но система пишет "No space left on device". В чем причина?**

```bash
Возможно, исчерпаны inodes (проверить df -i). 
Или файл был удален, но его держит открытым процесс (проверить `lsof | grep deleted`).
```
**23. Как найти файл по его содержимому?**
```bash
Команда grep -r "текст" /путь/для/поиска.
```

**24. Расскажите подробно о Cgroups.**
```yaml
    Control Groups (cgroups) — это механизм ядра Linux для ограничения, учета и изоляции использования ресурсов (CPU, RAM, Disk) группами процессов.
    Cgroups обеспечивают детерминированное распределение ресурсов в многопользовательских и контейнеризированных средах, предотвращая конфликты за ресурсы между различными рабочими нагрузками.
    Основные компоненты cgroups:
        Подсистемы (controllers) — отвечают за управление конкретным ресурсом.
        Иерархия (hierarchy) — дерево cgroups, где каждый узел (cgroup) может иметь свои ограничения.
        Процессы — привязываются к cgroup, и на них действуют ограничения этой cgroup.
    Подсистемы:
        cpu: ограничение использования CPU (например, с помощью cpu.shares или cpu.cfs_quota_us).
        memory: ограничение памяти (memory.limit_in_bytes) и подкачки.
        blkio: ограничение на операции ввода-вывода (блочные устройства).
        pids: ограничение количества процессов.
        net_cls: пометка сетевых пакетов для классификации трафика.
        devices: контроль доступа к устройствам.
```

**25. Разница между command1 & command2 и command1 && command2?**
```bash
    command1 & command2 – запуск command1 в фоновом режиме, а command2 – в основном, параллельно. 
    command1 && command2 – запуск command2 только если command1 завершился успешно (код возврата 0).
```
**26. Как проверить использование дискового пространства?**
```bash
    df -h – место на смонтированных ФС. du -sh /path/to/dir – размер, который занимает конкретная директория.
```

**27. Что такое cron и как настроить задание?**
```yaml
cron – демон для планирования периодических задач. 
Задание настраивается через crontab -e в формате: 
   *минута *час *день_месяца *месяц *день_недели команда.*
```

### Технологии хранения и виртуализации 🛠
**28. Что такое RAID и опишите уровни RAID 0, 1, 5, 10?**<br/>
```yaml
    RAID 0 (striping): скорость, без избыточности. 
    RAID 1 (mirroring): зеркалирование, надежность. 
    RAID 5: чередование + четность, баланс. 
    RAID 10: зеркало из массивов RAID 0, скорость + надежность.
```

**29. В чем разница между виртуализацией и контейнеризацией?**<br/>
```
    Виртуализация и контейнеризация — это два разных подхода к изоляции приложений и эффективному использованию ресурсов сервера. 
    Основное различие между ними заключается в уровне абстракции: виртуализация эмулирует целое аппаратное обеспечение для запуска полноценных ОС, 
    в то время как контейнеризация виртуализирует только операционную систему для запуска изолированных приложений.
```

**30. Что такое гипервизор и его типы?**<br/>
```yaml
    Гипервизор — это программное обеспечение, которое создает и запускает виртуальные машины (ВМ), позволяя нескольким операционным системам работать одновременно на одном физическом хосте
    Он выделяет каждой ВМ физические ресурсы (процессорное время, память, хранилище) и обеспечивает их изоляцию
    #Существует два основных типа гипервизоров
    Гипервизоры первого типа (аппаратные, "bare-metal"): 
    Устанавливаются непосредственно на "железо" сервера. Они более производительны и эффективны, так как взаимодействуют с оборудованием напрямую
    Примеры: VMware ESXi, Microsoft Hyper-V, KVM (на основе Linux), Xen

    Гипервизоры второго типа (хостовые) : 
    Работают как приложение внутри основной операционной системы (например, Windows или Linux). 
    Они проще в установке, но менее производительны из-за дополнительного слоя ОС.
    Примеры: Oracle VirtualBox, VMware Workstation, QEMU.
```

### Системные вызовы и низкоуровневые механизмы 🐧
**31. Что такое системный вызов (syscall) и как его отслеживать?**
```
    Программный способ запроса услуги от ядра ОС. Отслеживать: strace -e trace=<syscall_name> command или perf trace command.
```
**32. Какой системный вызов используется для создания процессов и потоков?**
```bash
    Процессы: fork()/vfork() + exec(). Потоки: clone() с флагами CLONE_VM, CLONE_FS, CLONE_FILES.
```
**33. Что такое cgroups (control groups) и для чего используются?**
```
    Механизм ядра для ограничения, учета и изолирования ресурсов (CPU, memory, disk I/O) для групп процессов. Основа контейнеризации.
```
**34. Что такое namespaces и какие виды namespaces существуют?**
```bash
    Механизм изоляции глобальных ресурсов (сеть, PID, mount points). 
    Виды: PID, Network, Mount, UTS, IPC, User, Cgroup, Time.
```
**35. В чем разница между kill и killall?**
```bash
    kill работает с PID процесса, 
    killall - с именем процесса. 
    Обе утилиты отправляют сигналы процессам.
```

### Системное администрирование 🔧

**36. Как найти все файлы с расширением .log размером больше 100MB?**
```bash
	find / -type f -name "*.log" -size +100M
```

**37. Как добавить задание в cron для выполнения каждый день в 2:30?**
```bash
	30 2 * * * /path/to/command в crontab (crontab -e)
```

**38. Что такое systemd и как управлять сервисами?**
```bash
	Современный init system и менеджер сервисов. 
    Команды: `systemctl (start|stop|restart|status) service_name`
```
**39. Как посмотреть последние 20 строк лог-файла в реальном времени?**
```bash
	tail -f -n 20 /var/log/file.log 
    journalctl -f -n 20 для systemd
```    
**40. Как найти все SUID/SGID файлы в системе?**
```bash
	find / -perm /6000 2>/dev/null
```

### 🌐 Сети (продвинутый)

**41. Что такое VXLAN и чем отличается от VLAN?**
```
VLAN - изоляция на уровне L2, только 4094 VLAN.
VXLAN (Virtual Extensible Local Area Network)- overlay сеть поверх IP (инкапсуляция), до 16 Mиллионов сегментов. 

```

**42. Как настроить статический маршрут в Linux?**
```bash
ip route add <network> via <gateway> или через /etc/network/interfaces/NetworkManager
```
**43. Что такое eBPF и для чего используется?**
```
eBPF (extended Berkeley Packet Filter) — это технология, 
    позволяющая безопасно запускать программы в пространстве ядра Linux без написания отдельных модулей ядра или перекомпиляции. 
    Программы eBPF выполняются в "песочнице", что гарантирует безопасность и стабильность работы системы.
```
**44. Как настроить bonding/teaming сетевых интерфейсов?**
```
    Через NetworkManager (nmcli) или конфиги в /etc/network/interfaces. Режимы: active-backup, balance-rr, 802.3ad.
```
**45. Что такое nftables и чем заменил iptables?**
```
Современная подсистема фильтрации пакетов, заменила iptables/ip6tables/arptables/ebtables. 
Единый синтаксис, лучшая производительность.
```
###  Контейнеризация и оркестрация 🐳

**46. Как ограничить память для Docker контейнера?**
```bash
    docker run --memory=512m image_name или в docker-compose: mem_limit: 512m
```
**47. Что такое Kubernetes pod?**
```
Наименьшая deployable единица в K8s - группа из одного или нескольких контейнеров с общим storage/network.
```
**48. Как посмотреть логи контейнера в Docker?**
```bash
    docker logs <container_id> или docker logs -f для follow
```

**49. В чем разница между ENTRYPOINT и CMD в Dockerfile?**
```yaml
    ENTRYPOINT: определяет исполняемую команду контейнера, которую сложно изменить. Контейнер всегда будет запускать эту программу.
    CMD: задает аргументы по умолчанию для ENTRYPOINT. Легко переопределяется при запуске контейнера.
    Ключевые различия:
    - ENTRYPOINT = "что запускать"
    - CMD = "с какими параметрами запускать"

    При одновременном использовании: 
    - ENTRYPOINT + CMD = полная команда
    - Аргументы в docker run переопределяют CMD
```
**50. В чём разница между COPY и ADD в Dockerfile?**
```yaml
    COPY: простая и предсказуемая команда, которая только копирует файлы и директории из хоста в образ. 
    Рекомендуется к использованию в большинстве случаев.

    ADD: более сложная команда с дополнительными возможностями:
        Копирование файлов (как COPY)
        Автоматическое распаковка архивов (tar, gzip, bzip2, xz)
        Загрузка файлов по URL

    - Почему две команды?
    Docker со временем пришел к принципу "явное лучше неявного". 
    ADD со своей "магией" распаковки может приводить к неожиданному поведению, 
    поэтому был введен COPY для простого и предсказуемого копирования.
```

### Безопасность 🔒

**51. Что такое AppArmor?**
```
    Система Mandatory Access Control (MAC). Профили определяют, к каким ресурсам имеет доступ приложение.
```
**52. Как настроить SSH tunneling?**
```bash
Локальный: ssh -L local_port:remote_host:remote_port user@jump_host. 
Удаленный: ssh -R remote_port:local_host:local_port user@remote_host
```
**53. Что такое mTLS (mutual TLS) и как настроить?**
```
    Двусторонняя аутентификация - клиент и сервер проверяют сертификаты друг друга. Настраивается в web-сервере (nginx, apache).
```
**54. Как настроить firewall с nftables?**
```bash
    nft add table inet filter
    nft add chain inet filter input { type filter hook input priority 0 \; }
    nft add rule inet filter input tcp dport 22 accept
```
**55. Что такое seccomp и для чего используется?**
```
    Secure computing mode - фильтр системных вызовов, ограничивает доступные syscall для процессов.
```
### 💾 Файловые системы и диски

**56. В чем разница между ext4 и XFS и btrfs?**
```bash
    ext4 - стабильность, журналирование. 
    XFS - высокая производительность с большими файлами, онлайн-расширение.
    BTRFS — это файловая система основанная на бинарных деревьях с функцией Copy on Write с снапшотами, компрессией, встроенным RAID, 
    ориентированная также на отказоустойчивость, восстановление и простоту администрирования.
```
**57. Как восстановить удаленный файл в Linux?**
```bash
    Через extundelete (для ext3/4) или testdisk. Требуется немедленное действие - не записывать на раздел.
```
**58. Что такое OverlayFS и как используется?**
```bash
    Union filesystem - объединяет несколько директорий (lower+upper). Основа storage драйверов в Docker.
```
**59. Как проверить целостность файловой системы?**	
```bash
    fsck /dev/sdX (предварительно размонтировать) или xfs_repair для XFS.
```
**60. Что такое LUKS encryption и как настроить?**
```bash
    Linux Unified Key Setup - прозрачное шифрование дисков. 
    Настройка: cryptsetup luksFormat /dev/sdX, cryptsetup open /dev/sdX name.
```
###  Производительность и мониторинг ⚡
**61. Как анализировать производительность дисковых операций?**
```bash
    iostat -x 1, iotop, fio для тестирования, blktrace для глубокого анализа.
```
**62. Что такое Memory Pressure и как отслеживать?**
```bash
    Нагрузка на подсистему памяти.
    ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem или cat /proc/pressure/memory
```
**63. Как настроить huge pages в Linux?**
```bash
    echo 1024 > /proc/sys/vm/nr_hugepages или через sysctl vm.nr_hugepages=1024
```
**64. Что такое swappiness и как настроить?**
```bash
    Параметр ядра (0-100), определяющий склонность к использованию swap. sysctl vm.swappiness=10
```

**65. Как отладить проблему с сетевой производительностью?**
```yaml
- iperf3 для тестирования пропускной способности, 
- netstat -i для ошибок, 
- ethtool для статистики интерфейса.
```
### Системное администрирование
 
**66. Как настроить NTP-клиент для синхронизации времени?**
```bash
    timedatectl set-ntp true (systemd) или через /etc/chrony.conf, /etc/ntp.conf
```

**67. Что такое Tmpfiles.d и для чего используется?**
```yaml
    Механизм systemd для управления временными файлами и директориями. 
    Конфиги в /etc/tmpfiles.d/*.conf
```
**68. Как создать RAM-диск (tmpfs) в Linux?**
```bash
    mount -t tmpfs -o size=1G tmpfs /mnt/ramdisk 
    или через /etc/fstab: tmpfs /mnt/ramdisk tmpfs size=1G 0 0
```
**69. Как линукс определяет что произошёл deadlock?**
```
    Linux имеет механизмы для обнаружения deadlock'ов, особенно в рамках подсистемы lockdep (lock dependency).
    Lockdep - это система отслеживания зависимостей блокировок, которая встроена в ядро Linux.
    Она не обнаруживает deadlock'ы в режиме реального времени, а скорее анализирует возможные условия deadlock'а во время выполнения кода,
    когда блокировки приобретаются и отпускаются.
    
    Как работает Lockdep:
    - Строит граф зависимостей блокировок в реальном времени
    - Отслеживает порядок захвата мьютексов, спинлоков, семафоров
    - Обнаруживает циклические зависимости между блокировками
```

**70. Как настроить ulimit для пользователя? Для чего нужно?**
```yaml
    Ulimits (User Limits) — это системные ограничения на использование ресурсов для процессов и пользователей в Linux.
    Что ограничивает:
        max user processes — максимальное количество процессов на пользователя
        open files — максимальное количество открытых файловых дескрипторов
        file size — максимальный размер создаваемых файлов
        stack size — максимальный размер стека процесса
        core file size — максимальный размер core-дампа при аварии
        virtual memory — максимальный объем виртуальной памяти
    Настраивается через /etc/security/limits.conf или systemd директивы LimitNOFILE, LimitNPROC
```

### Контейнеризация и Docker 🐳 
 
**71. В чем разница между docker exec и docker attach?**
```
    exec - выполняет новую команду в работающем контейнере. attach - подключается к основному процессу контейнера
```
**72. Как очистить все неиспользуемые Docker-ресурсы?**
```bash
    docker system prune -a - удаляет контейнеры, образы, сети, билд-кэш
```

**73. Что такое Docker layer и как оптимизировать образы?**
```
    Каждая инструкция в Dockerfile создает слой. Оптимизация: multi-stage билды, объединение RUN команд, .dockerignore
```
**74. Как настроить healthcheck в Docker?**
```
В Dockerfile: `HEALTHCHECK --interval=30s 
               CMD curl -f http://localhost/ exit 1`
```
**75. Что такое Docker buildkit и какие преимущества?**
```
	Новая система сборки образов: параллельная сборка, кэширование, секреты, улучшенная производительность
```

###  Kubernetes ☸️
 
**76. Как посмотреть логи пода в Kubernetes?**
```bash
    kubectl logs <pod-name> или kubectl logs -f <pod-name> для потокового вывода
```
**77. Что такое ConfigMap и Secret в Kubernetes?**
```
    ConfigMap - хранение конфигурационных данных. Secret - хранение чувствительных данных (base64 encoded)
```
**78. Как выполнить команду в работающем pod?**
```bash
    kubectl exec -it <pod-name> -- /bin/bash или нужная команда
```
**79. Что такое Ingress Controller и для чего используется?**
```
	Прокси-сервер (nginx, traefik) для маршрутизации HTTP-трафика в кластере на основе правил Ingress
```
**80. Как масштабировать deployment?**
```bash
	kubectl scale deployment/<name> --replicas=3
```    

###  Безопасность (продолжение) 🔒
 
**81. Что такое Falco и для чего используется?**
```
	Runtime security tool для обнаружения аномальной активности в контейнерах и хостах на основе правил
```
**82. Как настроить auditd для аудита системных событий?**
```bash
	Через /etc/audit/audit.rules: -w /etc/passwd -p wa -k identity_access
```
**83. Что такое CIS benchmarks и как применять?**
```
	Security best practices для ОС и софта. Инструменты: lynis, OpenSCAP для автоматической проверки
```    
**84. Как настроить PAM (Pluggable Authentication Modules)?**
```bash
	Конфиги в /etc/pam.d/. Модули: pam_tally2 для блокировки при неудачных входах, pam_limits для ограничений
```

**85. Что такое certificate pinning и как реализовать?**
```
	Фиксация ожидаемого сертификата/публичного ключа. Реализация: через HTTP-заголовки или на уровне приложения
```    

###  Сети  🌐
 
**86. Как настроить IPv6 в Linux?**
```bash
	ip -6 addr add 2001:db8::1/64 dev eth0 или через NetworkManager/systemd-networkd
```

**87. Что такое Anycast и как настроить?**
```
	Маршрутизация к "ближайшему" узлу. Настройка: BGP анонс одного префикса из нескольких локаций
```

**88. Как работает DNS resolution в Linux?**
```
	Через /etc/nsswitch.conf (порядок: files, dns) и /etc/resolv.conf (DNS серверы)
```
**89. Что такое WireGuard и как настроить?**
```
	Современный простой VPN. Настройка: wg-quick up wg0, конфиг в /etc/wireguard/wg0.conf
```    
**90. Как мониторить сетевой трафик?**
```
    tcpdump, tshark, iftop, nethogs. Для постоянного мониторинга - Prometheus + node_exporter
```

###  Производительность и мониторинг (продолжение) ⚡
 
**91. Что такое BPF Compiler Collection (BCC)?**
```
    Набор инструментов для анализа производительности и сетевого трафика на основе eBPF
```
**92. Как использовать perf для профилирования?**
```
    perf record -g command, perf report для анализа. perf stat для общей статистики
```
**93. Что такое USE Method для анализа производительности?**
```
	Utilization, Saturation, Errors - методология анализа ресурсов (CPU, memory, disk, network)
```

**94. Как настроить мониторинг с Prometheus?**
```
    Установка prometheus, настройка exporters (node_exporter), конфигурация scrape targets
```
**95. Что такое OpenTelemetry и как использовать?**
```
    Vendor-neutral framework для сбора телеметрии основанный на Elastic от AWS (metrics, logs, traces). Инструментирование приложений
```

###  DevOps инструменты 🛠
 
**96. Как настроить CI/CD pipeline в GitLab?**
```
    Через .gitlab-ci.yml в репозитории. Этапы: build, test, deploy. Использование runners
```
**97. Что такое Terraform state и как управлять?**
```
    Хранит состояние инфраструктуры. Бэкенды: local, S3, GCS, Azure Storage. Использование state locking
```
**98. Как настроить Ansible dynamic inventory?**
```
    Скрипт/программа, возвращающая JSON с хостами. Для cloud провайдеров - ec2.py, azure_rm.py
```

**99. Что такое GitOps и как реализовать?**
```
    Инфраструктура как код + pull-based деплой. Инструменты: ArgoCD, FluxCD
```

**100. Как настроить мониторинг логов в ELK/EFK stack?**
```
    Filebeat → Logstash/Fluentd → Elasticsearch → Kibana. Настройка парсинга, индексов, дашбордов
```

**101. Что такое service mesh (Istio, Linkerd)?**
```
    Слой инфраструктуры для управления service-to-service коммуникацией (traffic management, security, observability)
```

**102. Как настроить backup стратегию?**
```
    Инструменты: borgbackup, restic, rsnapshot. Стратегия: 3-2-1 правило (3 копии, 2 носителя, 1 оффсайт)
```

**103. Что такое immutable infrastructure?**
```
    Подход, где серверы не модифицируются после деплоя, а заменяются новыми при изменениях.
```

**104. Как реализовать zero-downtime деплой?**	
```bash
    Blue-green deployment, canary releases, rolling updates с proper health checks
```

**105. Что такое chaos engineering и инструменты?**
```
	Тестирование устойчивости системы через внесение сбоев. 
    Инструменты: Chaos Mesh, Litmus, Gremlin
```

### Продвинутое системное администрирование

**106. Как настроить Kubernetes cluster с kubeadm?**
```bash
	kubeadm init --pod-network-cidr=... #установка CNI, join worker nodes с токеном
```

**107. Что такое CoreDNS в Kubernetes и как настроить?**
```bash
	DNS-сервер для кластера. Конфиг через ConfigMap: kubectl edit configmap coredns -n kube-system
```
**108. Как настроить HPA (Horizontal Pod Autoscaler)?**
```bash
    kubectl autoscale deployment <name> --cpu-percent=50 --min=1 --max=10
```

**109. Что такое Kustomize и чем отличается от Helm?**
```
	Kustomize - template-free overlay система. Helm - менеджер пакетов с шаблонами. Оба для управления Kubernetes манифестами
```
**110. Как настроить network policies в Kubernetes?**
```
    Ресурс NetworkPolicy для контроля трафика между подами. Например, разрешить ingress только из определенных namespace
```

###  Безопасность контейнеров 🛡

**111. Что такое OPA/Gatekeeper в Kubernetes?**
```
	Open Policy Agent - валидация и enforcement политик безопасности в K8s через admission controllers
```
**112. Как настроить Pod Security Standards?**
```bash
	Через PodSecurity admission: kubectl label namespace default pod-security.kubernetes.io/enforce=baseline
```
**113. Что такое distroless образы и преимущества?**
```
	Минимальные образы без shell/package manager. Уменьшают attack surface и размер
```    
**114. Как сканировать образы на уязвимости?**
```
	Инструменты: trivy, grype, snyk. Интеграция в CI/CD pipeline
```    
**115. Что такое seccomp профили для Docker/K8s?**
```
	Ограничение системных вызовов. Настройка через securityContext.seccompProfile в Pod spec
```

### Cloud Native технологии ☁ 

**116. Как настроить AWS EKS cluster?**
```
	eksctl create cluster или через Terraform aws_eks_cluster resource
```
**117. Что такое service discovery в cloud?**
```
	Механизм автоматического обнаружения сервисов (Consul, etcd, cloud-native решения)
```

**118. Как настроить auto-scaling group в AWS?**
```
	Через Terraform: aws_autoscaling_group с launch template и scaling policies
```
**119. Что такое cloud-init и для чего используется?**
```
	Скрипт инициализации cloud instances. Настройка пользователей, пакетов, конфигов при старте
```

**120. Как мигрировать on-premise в cloud?**
```
	Стратегия: rehost (lift&shift), replatform, refactor. Инструменты: AWS MGN, Azure Migrate
```    

### 🔄 Git и CI/CD продвинутые

**121. Что такое Git sparse checkout?**
```bash
	git sparse-checkout init --cone - загрузка только части репозитория для больших mono-repo
```

**122. Как настроить GitLab runners autoscaling?**
```
	Docker machine executor или Kubernetes executor с auto-scaling на основе pending jobs
```

**123. Что такое ArgoCD и как настроить?**
```yaml
Declarative GitOps tool для K8s.
    Установка: 
    kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
```    

**124. Как реализовать canary deployment в K8s?**
```
	Через Istio/Service Mesh или Flagger с постепенным перенаправлением трафика
```

**125. Что такое Хуйнянейм?**
```
	
```

### 📊 Мониторинг и observability

**126. Как настроить Grafana Loki для логов?**
```
	helm install loki grafana/loki-stack - log aggregation система, аналогичная Prometheus для метрик
```    
**127. Что такое OpenMetrics стандарт?**
```
	Стандарт формата для метрик, расширяющий Prometheus format
```    
**128. Как настроить distributed tracing?**
```
	Инструменты: Jaeger, Zipkin. Инструментирование приложений через OpenTelemetry
```    
**129. Что такое RED метрики?**
```
	Rate, Errors, Duration - ключевые метрики для мониторинга сервисов
```

**130. Как настроить alertmanager в Prometheus?**
```
	Конфигурация routes, receivers, inhibition rules в alertmanager.yml
```    
###  Производительность и оптимизация 

**131. Что такое eBPF и применение в мониторинге?**
```
	Extended Berkeley Packet Filter - программы в ядре для безопасного мониторинга без модификации кода
```    
**132. Как оптимизировать Linux для high performance?**
```
	Настройка: sysctl params (net.core.somaxconn, vm.swappiness), CPU governor, I/O scheduler
```    
**133. Что такое memory compaction и fragmentation?**
```
	Проблемы управления памятью. Решения: huge pages, defragmentation tools
```    
**134. Как настроить CPU pinning в K8s?**
```yaml
    Через cpuManagerPolicy: static в kubelet и requests.cpu в Pod spec
```
**135. Что такое irqbalance и для чего?**
```
	Демон для балансировки прерываний между CPU ядрами. Критично для сетевой производительности
```
### Инфраструктура как код

**136. Как использовать Terraform workspaces?**
```
	terraform workspace new dev - изоляция state для разных окружений
```
**137. Что такое Terragrunt и преимущества?**
```
	Обертка над Terraform для устранения повторения кода, управления зависимостями
```
**138. Как настроить Terraform remote state?**
```
    Бэкенд S3/GCS/Azure с state locking через DynamoDB/Cloud Storage
```
**139. Что такое Crossplane в Kubernetes?**
```
    Framework для управления cloud resources через K8s API (Infrastructure as Code)
```
**140. Как мигрировать state в Terraform?**
```
	terraform state mv для перемещения ресурсов между state файлами
```

### 🛠 Продвинутые инструменты
**141. Что такое WebAssembly (WASM) в Kubernetes?**
```
	Krustlet - запуск WASM workloads вместо containers. Преимущества: безопасность, переносимость
```    
**142. Как использовать Buildpacks вместо Dockerfile?**
```
	pack build - автоматическая сборка образов без написания Dockerfile
```
**143. Что такое Dagger для CI/CD?**
```
	Portable CI/CD engine как code. Запуск pipelines в containers, кэширование, параллелизация
```
**144. Как настроить confidential computing?**
```
	Технологии: Intel SGX, AMD SEV. Защита данных в процессе выполнения
```
**145. Что такое FinOps и инструменты?**
```
	Cloud financial management. Инструменты: Infracost, Kubecost, CloudHealth
```    

### Стратегия и best practices 🎯

**146. Как организовать multi-cluster management?**
```
	Инструменты: Fleet, Karmada. Единое управление несколькими K8s кластерами
```
**147. Что такое platform engineering?**
```
	Создание внутренних developer platforms (IDP) с самообслуживанием для разработчиков
```    
**148. Как реализовать policy as code?**
```
	Инструменты: OPA, Kyverno. Автоматическое enforcement политик безопасности и compliance
```    
**149. Что такое MLOps и отличия от DevOps?**
```
	DevOps для ML: управление данными, эксперименты, model versioning, monitoring drift
```
**150. Как построить SRE практику в компании?**
```
	SLI/SLO/SLA определение, error budgets, blameless postmortems, automation
    
```

------------------------------------
## SRE section
------------------------------------

**SLI (Service Level Indicator)** - это конкретный измеримый показатель надежности сервиса. Например, процент успешных HTTP-запросов, задержка ответа или доступность сервиса. SLI отвечает на вопрос "Что мы измеряем?".

**SLO (Service Level Objective)** - целевое значение для SLI за определенный период времени. Например, "99.9% запросов должны быть успешными в течение месяца". SLO определяет, какой уровень надежности мы считаем приемлемым.

**SLA (Service Level Agreement)** - формальное соглашение с пользователями, основанное на SLO. В отличие от SLO, SLA включает последствия при нарушении целевых показателей, например, финансовые компенсации или штрафы.

### Error Budget

**Error Budget** - это допустимый предел ошибок, рассчитываемый из SLO. Если SLO доступности составляет 99.9%, то Error Budget равен 0.1%. Это означает, что сервис может быть недоступен до 0.1% времени без нарушения SLO. Error Budget создает баланс между надежностью и скоростью разработки - команда может "тратить" бюджет на внесение изменений и новых функций.

## MTTR и MTTD

**MTTD (Mean Time To Detect)** - среднее время между возникновением инцидента и его обнаружением системой мониторинга или командой. Низкий MTTD означает быструю идентификацию проблем.

**MTTR (Mean Time To Restore)** - среднее время от обнаружения инцидента до полного восстановления работы сервиса. MTTR включает в себя время на диагностику, исправление и проверку решения. В некоторых контекстах MTTR может также означать Mean Time To Repair или Mean Time To Resolve.

Эти метрики вместе помогают командам количественно оценивать и улучшать надежность сервисов, балансируя между стабильностью и скоростью разработки.

------------------------------------

**RPO (Recovery Point Objective)** — целевая точка восстановления. Это максимальный период времени, на который можно допустить потерю данных при аварии. Например, если RPO = 1 час, то при восстановлении после сбоя данные должны быть актуальными на момент не позже чем 1 час назад. **RPO отвечает на вопрос: "Сколько данных мы можем позволить себе потерять?"**

**RTO (Recovery Time Objective)** — целевое время восстановления. Это максимально допустимое время простоя системы после аварии до момента возобновления её работы. Например, если RTO = 4 часа, то система должна быть полностью восстановлена и работоспособна в течение этого времени. **RTO отвечает на вопрос: "Как быстро мы должны восстановить работу сервиса?"**

**Простыми словами:** 
- **RPO** — это про **данные** (сколько потеряем)
- **RTO** — это про **время простоя** (как долго не работаем)

Эти показатели являются ключевыми при проектировании систем аварийного восстановления (Disaster Recovery) и определяют требования к резервному копированию и инфраструктуре высокой доступности.

------------------------------------

## Дополнительный материал 📚

### Что такое ядро ОС и его роль
*Ядро операционной системы — это центральная, самая главная ее часть*
*Оно работает на самом низком уровне и выступает в роли моста между программным обеспечением (приложениями) и физическим оборудованием (аппаратурой)*

Основные функции ядра:
```yaml
Управление процессами: Создание, планирование и завершение процессов (задач)
Управление памятью: Выделение и освобождение оперативной памяти для программ
Управление устройствами: Обеспечение работы с оборудованием (дисками, сетью, клавиатурой) через драйвер
Обработка прерываний: Немедленная реакция на срочные события от аппаратных компонентов
Обеспечение системной безопасности: Контроль доступа к ресурсам
```
### Библиотеки и технологии виртуализации в Linux
```yaml
KVM (Kernel-based Virtual Machine) : Это не библиотека, а модуль ядра Linux, который превращает его в гипервизор первого типа
KVM отвечает за непосредственное выполнение кода гостевых ОС на процессоре, используя аппаратную поддержку виртуализации (Intel VT или AMD-V)
Libvirt : Это набор инструментов и API-интерфейсов для единообразного управления различными технологиями виртуализации, такими как KVM, Xen, QEMU и LXC
Такие утилиты, как virsh (командная строка) и virt-manager (графический интерфейс), являются фронтендами к libvirt
QEMU : Универсальный эмулятор, который может работать независимо, эмулируя разные архитектуры процессоров. В связке с KVM он выступает в роли эмулятора виртуальных устройств (сетевых карт, дисков), в то время как KVM обеспечивает высокоскоростное выполнение кода
LXC (Linux Containers) : Технология контейнеризации на уровне операционной системы, которая для изоляции использует механизмы ядра Linux (пространства имен - namespaces и группы управления - cgroups)
Это "родная" для Linux технология контейнеров, более легковесная, чем виртуальные машины.
```

## Системы инициализации в Linux

В Linux, помимо `systemd`, существует несколько систем инициализации, каждая из которых основана на своей философии. 
В то время как `systemd` стремится быть универсальным и функциональным решением, альтернативы часто делают акцент на простоте, модульности или совместимости.

*Для быстрого сравнения основных систем инициализации можно воспользоваться этой таблицей.*
###  Сравнение систем инициализации 📊
![initsys](img/img1.jpg)

###  Более подробный обзор альтернатив

- **OpenRC**: Используется в Gentoo, Alpine Linux и других. Это эволюционное развитие SysVinit, которое добавляет асинхронный запуск служб и управление зависимостями, но при этом сохраняет привычную структуру скриптов в `/etc/init.d/`.
- **Runit**: Состоит из небольшого набора утилит, которые выполняют три четкие задачи: инициализация системы, наблюдение за службами и завершение работы. Его надежность и простота сделали его выбором для таких дистрибутивов, как Void Linux.
- **s6**: Это не единая программа, а набор инструментов для построения собственной системы инициализации и наблюдения за процессами. Ориентирован на безопасность и модульность, что дает полный контроль, но требует глубоких знаний для настройки.

------------------------------------
###  Создание процессов и потоков 🖥️
Создание процессов и управление памятью в Linux — это многоуровневый процесс, в котором участвуют системные вызовы ядра и менеджер виртуальной памяти. В основе лежит разделение на **виртуальное адресное пространство каждого процесса** и **физическую память**.

Новые процессы в Linux создаются в основном с помощью системного вызова `fork()`. При этом создается точная копия родительского процесса-со всеми его ресурсами. Затем процесс часто использует вызов `exec()`, чтобы загрузить и выполнить новую программу.

| Аспект           |                         Процессы                           |                          Потоки (нити)                                     |
| ---------------- |----------------------------------------------------------- | -------------------------------------------------------------------------- |
| **Изоляция**     | Полная изоляция адресных пространств.                      | Разделяют общее адресное пространство процесса.                            |
| **Создание**     | Системный вызов `fork()`.                                  | Системный вызов `clone()` с определенными флагами или библиотека `pthreads`|
| **Коммуникация** | Межпроцессное взаимодействие (IPC): сигналы, каналы.       | Прямой доступ к общим данным процесса (требует синхронизации).             |

Потоки, или легковесные процессы (LWP), создаются с помощью системного вызова `clone()`. Ключевое отличие — флаги, которые указывают, какие ресурсы родителя будет разделять новый поток. Если указан флаг `CLONE_VM`, поток разделяет адресное пространство с родителем, в противном случае создается новый процесс. Библиотека `pthreads` предоставляет высокоуровневый интерфейс для работы с потоками, внутри также используя `clone()`.

### Организация виртуальной памяти процесса 🗺️

Каждому процессу в Linux выделяется собственное виртуальное адресное пространство, которое обычно делится на две части: **пространство ядра** (общее для всех процессов) и **пользовательское пространство** (уникальное для каждого процесса).

Пользовательское пространство процесса организовано в сегменты:

*   **Сегмент кода (Text)**: Содержит исполняемые инструкции программы. Доступен только для чтения.
*   **Сегмент данных (Data) и BSS**: `Data` содержит статические инициализированные переменные. `BSS` содержит статические неинициализированные переменные.
*   **Куча (Heap)**: Область для динамического выделения памяти (например, с помощью `malloc`). 
    Растет в сторону старших адресов. Размер кучи увеличивается системным вызовом `brk()` или `sbrk()`.
*   **Стек (Stack)**: Используется для хранения локальных переменных и адресов возврата из функций.
    Растет в сторону младших адресов. Каждый поток в процессе имеет свой собственный стек.
*   **Сегмент отображения файлов (Memory Mapping Segment)**: В этой области с помощью `mmap()` отображаются в память динамические библиотеки, файлы данных, а также создаются анонимные отображения для крупных блоков памяти.

Соответствие между виртуальными адресами и физической памятью описывается с помощью **таблиц страниц (page table)**. 
Каждый процесс имеет свой собственный набор таблиц страниц.

### Ключевые механизмы ядра ⚙️

*   **Copy-on-Write (CoW)**: При вызове `fork()` родительский и дочерний процессы изначально используют одни и те же физические страницы памяти, помеченные как "только для чтения". Когда один из процессов пытается выполнить запись, возникает исключение, и ядро в этот момент создает для него отдельную копию изменяемой страницы. Это позволяет экономить память и ускоряет создание процессов.
*   **Кэширование и буферизация**: Ядро Linux стремится использовать всю свободную физическую память для кэширования дисковых операций и буферизации данных. Когда приложениям требуется больше памяти, ядро освобождает часть кэшированных данных.
*   **Overcommitting памяти**: Ядро может разрешить процессам выделять больше виртуальной памяти, чем есть доступной физической памяти + места в подкачке. Это основано на предположении, что не все процессы используют всю выделенную им память одновременно.

------------------------------------

Технология eBPF — это революционный механизм ядра Linux, а Calico и Cilium — два популярных решения для сетевого взаимодействия и безопасности в Kubernetes, которые по-разному её используют.

### Что такое eBPF и где применяется 🛠️

**eBPF (extended Berkeley Packet Filter)** — это технология, позволяющая безопасно запускать программы в пространстве ядра Linux без написания отдельных модулей ядра или перекомпиляции. Программы eBPF выполняются в "песочнице", что гарантирует безопасность и стабильность работы системы.

**Ключевые возможности eBPF:**

*   **Безопасность**: Позволяет перехватывать системные вызовы, обрабатывать сетевые пакеты и собирать информацию о сетевых операциях на уровне сокетов, что помогает в создании систем мониторинга и безопасности.
*   **Сетевое взаимодействие**: Обработка сетевых пакетов до их поступления в сетевую подсистему Linux (через XDP - eXpress Data Path). Используется для защиты от DDoS-атак и маршрутизации трафика.
*   **Наблюдаемость и профилирование**: Можно прикрепляться к работающим процессам, получать доступ к стеку, куче и переменным функций, что даёт глубокое представление о поведении системы.

###  Сравнение Calico и Cilium 🔄

Несмотря на схожие цели, Calico и Cilium существенно различаются по архитектуре и подходу.
 
| **Аспект**         |                         **Calico**                               |                              **Cilium**                             |
| ------------------ | ---------------------------------------------------------------- | ------------------------------------------------------------------- |
| **Сетевая модель** | Реализует плоскую сеть L3 с маршрутизацией BGP.                  | Использует eBPF для управления сетевым трафиком и политиками.       |
| **Архитектура**    | Гибкая, поддерживает eBPF, Linux IPtables, Windows HNS.          | Единая eBPF-based архитектура.                                      |
| **Безопасность**   | Политики на уровне сети (L3/L4), модель "наименьших привилегий". | Identity-based безопасность на метках Kubernetes, фильтрация на L7. |
| **Экосистема**     | Широкая: Kubernetes, OpenShift, Docker EE, OpenStack.            | Фокус на Kubernetes и совместимых платформах.                       |

**Calico** использует протокол BGP, где каждый узел Kubernetes выступает в роли маршрутизатора, объявляя маршруты для своих Pod'ов. При использовании eBPF Calico сохраняет гибкость, позволяя переключаться между плоскостями данных.

**Cilium** с самого начала спроектирован вокруг eBPF, что позволяет ему обеспечивать более глубокую наблюдаемость (включая уровень L7) и управлять сетевыми политиками на основе идентификаторов рабочих нагрузок, а не просто IP-адресов.

Ваш выбор между Calico и Cilium должен зависеть от конкретных требований инфраструктуры:

*   **Выбирайте Cilium**, если вам нужны расширенные возможности безопасности на уровне L7, глубокая наблюдаемость и вы готовы принять модель, ориентированную исключительно на eBPF и Kubernetes.
*   **Выбирайте Calico**, если вам важна гибкость, поддержка разнородных сред (виртуальные машины, Kubernetes, bare-metal) или вы предпочитаете проверенную модель маршрутизации BGP.
------------------


 # Firewall и ACID CAP
------------------

## 1. Firewall в Linux: архитектура и эволюция

**Основа:** Современные фаерволы Linux работают на подсистеме **netfilter** в ядре.

### **Архитектура netfilter:**

```
Пакет → [HOOK Points: PREROUTING → FORWARD → POSTROUTING]
               ↓
          [INPUT/OUTPUT]
```

**Хуки (Hook Points)** - точки перехвата пакетов:
- **PREROUTING** - пакеты приходят с сети
- **INPUT** - пакеты для локальных процессов  
- **FORWARD** - транзитные пакеты
- **OUTPUT** - пакеты от локальных процессов
- **POSTROUTING** - пакеты перед отправкой в сеть

### **Эволюция инструментов:**

| Поколение | Инструмент | Описание |
|-----------|------------|----------|
| **1-2** | `ipchains` | Устаревший, сложные правила |
| **3** | `iptables` | Классический, таблицы: filter, nat, mangle |
| **4** | `nftables` | Современный унифицированный синтаксис |

### **Пример перехода с iptables на nftables:**

```bash
# iptables - блокировка порта 80
iptables -A INPUT -p tcp --dport 80 -j DROP

# nftables - эквивалент
nft add rule inet filter input tcp dport 80 drop
```

**Ключевые преимущества nftables:**
- Единый синтаксис для IPv4/IPv6
- Лучшая производительность
- Упрощенная конфигурация
- Динамические правила

---

## 2. ENTRYPOINT vs CMD в Docker: детальные отличия
### **Базовая семантика:**

```dockerfile
# Формы записи:
ENTRYPOINT ["executable", "param1"]  # exec форма (рекомендуется)
ENTRYPOINT command param1             # shell форма

CMD ["executable", "param1"]         # exec форма  
CMD command param1                    # shell форма
```

### **Сравнительная таблица:**

| Аспект | ENTRYPOINT | CMD |
|--------|------------|-----|
| **Назначение** | Исполняемая программа | Аргументы по умолчанию |
| **Переопределение** | `--entrypoint` флаг | Аргументы в `docker run` |
| **Объединение** | Исполняется + CMD как аргументы | Игнорируется если есть ENTRYPOINT |
| **Shell-форма** | Запускает через `/bin/sh -c` | Запускает через `/bin/sh -c` |

### **Практические сценарии:**

**Сценарий 1: ENTRYPOINT как исполняемый файл**
```dockerfile
FROM alpine
ENTRYPOINT ["/bin/ping"]
CMD ["localhost"]

# docker run image → /bin/ping localhost
# docker run image google.com → /bin/ping google.com
```

**Сценарий 2: Только CMD для временных контейнеров**
```dockerfile
FROM ubuntu
CMD ["/bin/bash"]

# Легко переопределить: docker run image top
```

**Сценарий 3: Скрипт-обертка в ENTRYPOINT**
```dockerfile
FROM postgres
COPY docker-entrypoint.sh /
ENTRYPOINT ["/docker-entrypoint.sh"]
CMD ["postgres"]
```

**Критическое правило:** При одновременном использовании ENTRYPOINT и CMD в exec-форме, CMD становится аргументами для ENTRYPOINT.
---

## 3. Виды репликации реляционных БД

### **Классификация по механизму:**
#### **1. Физическая репликация (Physical/Binary)**
- **Принцип:** Копирование бинарных WAL (Write-Ahead Log) записей
- **Примеры:** PostgreSQL streaming replication, Oracle Data Guard
- **Преимущества:**
  - Бит-в-бит идентичность
  - Минимальная задержка
  - Простота настройки
- **Недостатки:**
  - Версии должны совпадать
  - Нет выборочной репликации

#### **2. Логическая репликация (Logical/Statement-based)**
- **Принцип:** Передача SQL-операций (INSERT/UPDATE/DELETE)
- **Примеры:** PostgreSQL logical replication, MySQL binlog replication
- **Преимущества:**
  - Разные версии БД
  - Выборочная репликация таблиц
  - Возможность трансформации данных
- **Недостатки:**
  - Больше накладных расходов
  - Риск конфликтов репликации

### **Топологии репликации:**

| Топология | Описание | Use-case |
|-----------|----------|----------|
| **Master-Slave** | Одна запись, много чтения | Чтение-ориентированные workload |
| **Multi-Master** | Запись в любой узел | Высокая доступность |
| **Chain** | Каскадная репликация | Гео-распределение |
| **Star** | Центральный мастер | Упрощенное управление |

### **Синхронность:**

- **Синхронная:** Гарантия сохранности данных, высокая задержка
- **Асинхронная:** Лучшая производительность, риск потери данных
- **Полу-синхронная:** Баланс между производительностью и надежностью

---
## 4. ACID vs CAP: фундаментальные отличия

### **ACID (Свойства транзакций):**
```sql
BEGIN TRANSACTION;
UPDATE accounts SET balance = balance - 100 WHERE user_id = 1;
UPDATE accounts SET balance = balance + 100 WHERE user_id = 2;
COMMIT; -- Все или ничего
```

**Компоненты:**
- **Atomicity** (Атомарность) - Все операции транзакции выполняются или ни одна
- **Consistency** (Согласованность) - Данные переходят из одного валидного состояния в другое
- **Isolation** (Изолированность) - Параллельные транзакции не мешают друг другу
- **Durability** (Долговечность) - Результаты фиксированной транзакции сохраняются после сбоя

### **CAP Theorem (Ограничения распределенных систем):**

**Формулировка:** В распределенной системе можно гарантировать только 2 из 3 свойств:

- **Consistency** (Согласованность) - Все узлы видят одни и те же данные в один момент времени
- **Availability** (Доступность) - Каждый запрос получает ответ (успех или ошибка)
- **Partition Tolerance** (Устойчивость к разделению) - Система работает при сетевых разрывах

### **Сравнительная таблица:**

| Аспект                 |                ACID              |                    CAP                  |
| ---------------------- | -------------------------------- | --------------------------------------- | 
| **Область**            | Транзакции в одной БД            |  Распределенные системы                 |
| **Consistency**        | Согласованность состояния данных | Линейная консистентность между узлами   |
| **Временной контекст** | Момент выполнения транзакции     | Состояние системы в произвольный момент |
| **Тип гарантий**       | Строгие математические           | Теорема-ограничение                     |

### **Практические реализации:**

**ACID-системы:**
- PostgreSQL, Oracle, MySQL InnoDB
- **Жертвуют:** Масштабируемостью записи

**CAP-системы:**
- **CP** (Consistency + Partition Tolerance): PostgreSQL с синхронной репликацией, MongoDB
- **AP** (Availability + Partition Tolerance): Cassandra, DynamoDB
- **CA** (теоретически): Одиночные узлы БД

### **PACELC-расширение:**
Более практичная модель, учитывающая нормальную работу:
- **При сетевом разделении (P):** Выбор между A и C
- **В нормальных условиях (ELC):** Выбор между L (latency) и C (consistency)

**Примеры:**
- **MongoDB:** PA/EL (приоритет доступности и низкой задержки)
- **Cassandra:** PA/EL  
- **PostgreSQL:** PC/EC (приоритет согласованности)

### **Вывод:**
ACID и CAP оперируют на разных уровнях: ACID - гарантии для одиночных операций, 
CAP - фундаментальные ограничения распределенных систем. 
Современные БД находят компромиссы между этими моделями через различные уровни изоляции и консистентности.

------------------

------------------
## **Топ-15 сигналов Linux по практической значимости:**
------------------

### **Критические системные сигналы**

1. **SIGKILL (9)** - Безусловное убийство процесса (не перехватывается)
2. **SIGTERM (15)** - Корректное завершение (graceful shutdown)
3. **SIGHUP (1)** - Hangup (перезапуск демонов, обновление конфигурации)
4. **SIGINT (2)** - Прерывание с терминала (Ctrl+C)
5. **SIGQUIT (3)** - Выход с core dump (Ctrl+\)
---
### **Сигналы ошибок выполнения**

6. **SIGSEGV (11)** - Segmentation fault (нарушение доступа к памяти)
7. **SIGILL (4)** - Illegal instruction (недопустимая инструкция CPU)
8. **SIGFPE (8)** - Floating point exception (арифметическая ошибка)
9. **SIGBUS (7)** - Bus error (невыровненный доступ к памяти)
10. **SIGABRT (6)** - Abort (аварийное завершение от abort())
---
### **Сигналы управления процессами**
11. **SIGCHLD (17)** - Child process terminated (изменение состояния потомка)
12. **SIGSTOP (19)** - Приостановка выполнения (не перехватывается)
13. **SIGCONT (18)** - Продолжение выполнения
14. **SIGTSTP (20)** - Stop from terminal (Ctrl+Z)
---

## **Специализированные сигналы**
15. **SIGUSR1 (10)** - Пользовательский сигнал 1
    **SIGUSR2 (12)** - Пользовательский сигнал 2
---
### **Быстрая справка по номерам:**
```bash
# Просмотр всех сигналов
kill -l
# Отправка сигнала процессу
kill -9 PID      # SIGKILL
kill -15 PID     # SIGTERM  
kill -1 PID      # SIGHUP
```
**Примечание:** SIGUSR1/2 часто используются для кастомной логики (reopen logs, reload config).



------------------
