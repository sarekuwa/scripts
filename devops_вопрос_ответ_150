Вот подборка из 150 актуальных вопросов и ответов по Linux для собеседования в DevOps, которые охватывают ключевые темы, с которыми вы столкнетесь в 2025 году.

### 🐧 Управление процессами и производительностью

| Вопрос | Краткий ответ |
| :--- | :--- |
| 1. Что такое Load Average (средняя нагрузка)? | Среднее количество процессов в состоянии **R** (ожидание CPU) и **D** (непрерываемый сон) за 1, 5 и 15 минут. Показывает нагрузку на систему относительно числа CPU-ядер. |
| 2. Что такое Zombie-процесс (процесс-зомби)? | Процесс, который завершил выполнение (`exit()`), но его запись еще существует в таблице процессов, так как родитель не прочитал его статус завершения через `wait()`. |
| 3. Какой процесс имеет PID 1 и за что он отвечает? | Это процесс `systemd` (или `init` в старых системах). Он является родителем всех процессов, отвечает за загрузку системы и управление службами. |
| 4. В чем разница между процессом и потоком? | **Процесс** – изолированный экземпляр программы с собственными ресурсами. **Поток** – часть процесса, делит с ним память и ресурсы, но имеет собственный стек и счетчик команд. |
| 5. Как найти и завершить процесс? | Найти PID: `pgrep <name>` или `ps aux | grep <name>`. Завершить: `kill <PID>` (посылает SIGTERM) или `kill -9 <PID>` (SIGKILL, принудительное завершение). |
| 6. Что такое `nohup` и для чего он используется? | `nohup` позволяет команде продолжать работу после завершения терминала или сессии пользователя, игнорируя сигнал `SIGHUP` (Hangup). |

### 💾 Файловые системы и диски

| Вопрос | Краткий ответ |
| :--- | :--- |
| 7. В чем разница между жесткой (hard link) и символьной (soft/symbolic link) ссылкой? | **Жесткая** – еще одно имя для тех же данных (inode). Нельзя ссылаться на каталоги или другие файловые системы. **Символьная** – файл-указатель на путь, как ярлык. Может ссылаться на что угодно. |
| 8. Что такое inode? | Индексный дескриптор – структура данных, хранящая метаинформацию о файле (права, владелец, timestamps), кроме его имени и содержимого. |
| 9. Что такое LVM (Logical Volume Manager) и зачем он нужен? | Технология для абстракции дискового пространства. Позволяет объединять несколько дисков в одну группу томов (VG) и гибко создавать/изменять размер логических томов (LV) на лету. |
| 10. Что такое Swap Space (пространство подкачки)? | Область на диске, используемая ядром как "дополнительная RAM" когда физической памяти не хватает. Реализуется как отдельный раздел или файл. |
| 11. Как увеличить размер существующей файловой системы? | Зависит от конфигурации. Обычно: 1) Увеличить базовый диск/LVM-том. 2) Использовать `resize2fs` (для ext*) или `xfs_growfs` (для XFS) для расширения ФС. |
| 12. Команда `cat file1 > file2` и `cat file1 >> file2`? | `>` – перенаправление вывода с **перезаписью** файла `file2`. `>>` – перенаправление вывода с **дозаписью** в конец файла `file2`. |
| 13. Разница между `Ctrl+C` и `Ctrl+Z` в терминале? | `Ctrl+C` посылает сигнал `SIGINT` (завершение процесса). `Ctrl+Z` посылает сигнал `SIGTSTP` (приостановка процесса и помещение его в фоновый режим). |

### 🔐 Права доступа и безопасность

| Вопрос | Краткий ответ |
| :--- | :--- |
| 14. Что означают права 755 на файл? | `rwxr-xr-x`. Владелец: читать, писать, выполнять. Группа: читать, выполнять. Остальные: читать, выполнять. (Чтение=4, Запись=2, Выполнение=1). |
| 15. Зачем директории нужен право на выполнение (`x`)? | Право на выполнение (`x`) для каталога дает возможность войти в него (сделать его текущим рабочим каталогом) и получить доступ к файлам внутри него. |
| 16. Что такое SELinux? | SELinux (Security-Enhanced Linux) – это система принудительного контроля доступа (MAC), которая разграничивает доступ процессов к ресурсам на основе политик безопасности, дополняя стандартные права доступа. |
| 17. Как организовать доступ по SSH без пароля? | Сгенерировать пару SSH-ключей на клиенте (`ssh-keygen`). Скопировать публичный ключ на сервер в `~/.ssh/authorized_keys` (команда `ssh-copy-id`). |

### 🌐 Сети и управление пакетами

| Вопрос | Краткий ответ |
| :--- | :--- |
| 18. Как проверить, открыт ли порт на удаленном хосте? | Использовать `telnet <host> <port>`, `nc -zv <host> <port>` или `nmap -p <port> <host>`. |
| 19. Как проверить сетевую связность? | Команда `ping <host>` для проверки доступности и `traceroute <host>` для прослеживания маршрута. |
| 20. Что такое фаервол (firewall) в Linux? | Программа или компонент ядра, который фильтрует сетевой трафик (входящий, исходящий, транзитный) на основе заданных правил. Современный стандарт – `nftables`, ранее `iptables`. |
| 21. Какой пакетный менеджер используется в Debian/Ubuntu и RedHat/CentOS? | **Debian/Ubuntu**: `apt` или `apt-get`. **RedHat/CentOS**: `yum` (старые версии) или `dnf` (новые версии). |

### ⚙️ Администрирование и устранение неисправностей

| Вопрос | Краткий ответ |
| :--- | :--- |
| 22. Ситуация: `df -h` показывает свободное место, но система пишет "No space left on device". В чем причина? | Возможно, исчерпаны **inodes** (проверить `df -i`). Или файл был удален, но его держит открытым процесс (проверить `lsof / | grep deleted`). |
| 23. Как найти файл по его содержимому? | Команда `grep -r "текст" /путь/для/поиска`. |
| 24. Как посмотреть, кто и что использует удаленный файл, который был удален? | Команда `lsof / | grep deleted` покажет процессы, которые держат открытыми удаленные файлы. |
| 25. Разница между `command1 & command2` и `command1 && command2`? | `command1 & command2` – запуск `command1` в **фоновом режиме**, а `command2` – в основном, параллельно. `command1 && command2` – запуск `command2` **только если** `command1` завершился успешно (код возврата 0). |
| 26. Как проверить использование дискового пространства? | `df -h` – место на смонтированных ФС. `du -sh /path/to/dir` – размер, который занимает конкретная директория. |
| 27. Что такое `cron` и как настроить задание? | `cron` – демон для планирования периодических задач. Задание настраивается через `crontab -e` в формате: `минута час день_месяца месяц день_недели команда`. |

### 🛠 Технологии хранения и виртуализации

| Вопрос | Краткий ответ |
| :--- | :--- |
| 28. Что такое RAID и опишите уровни RAID 0, 1, 5, 10? | **RAID 0** (striping): скорость, без избыточности. **RAID 1** (mirroring): зеркалирование, надежность. **RAID 5**: чередование + четность, баланс. **RAID 10**: зеркало из массивов RAID 0, скорость + надежность. |
| 29. В чем разница между виртуализацией и контейнеризацией? | **Виртуализация** – эмуляция полноценной ОС со своим ядром поверх гипервизора. **Контейнеризация** (Docker) – изоляция процессов с использованием ядра хоста, более легковесна. |
| 30. Как при запуске Docker-контейнера "пробросить" порт с хоста в контейнер? | Флаг `-p` в команде `docker run`. Пример: `docker run -p 8080:80 nginx` пробрасывает порт 8080 хоста на порт 80 контейнера. |

## 🐧 Системные вызовы и низкоуровневые механизмы

| Вопрос | Краткий ответ |
| :--- | :--- |
| 31. Что такое системный вызов (syscall) и как его отслеживать? | Программный способ запроса услуги от ядра ОС. Отслеживать: `strace -e trace=<syscall_name> command` или `perf trace command`. |
| 32. Какой системный вызов используется для создания процессов и потоков? | **Процессы**: `fork()`/`vfork()` + `exec()`. **Потоки**: `clone()` с флагами `CLONE_VM`, `CLONE_FS`, `CLONE_FILES`. |
| 33. Что такое cgroups (control groups) и для чего используются? | Механизм ядра для ограничения, учета и изолирования ресурсов (CPU, memory, disk I/O) для групп процессов. Основа контейнеризации. |
| 34. Что такое namespaces и какие виды namespaces существуют? | Механизм изоляции глобальных ресурсов (сеть, PID, mount points). Виды: PID, Network, Mount, UTS, IPC, User, Cgroup, Time. |
| 35. В чем разница между `kill` и `killall`? | `kill` работает с PID процесса, `killall` - с именем процесса. Обе утилиты отправляют сигналы процессам. |

## 🔧 Системное администрирование

| Вопрос | Краткий ответ |
| :--- | :--- |
| 36. Как найти все файлы с расширением `.log` размером больше 100MB? | `find / -type f -name "*.log" -size +100M` |
| 37. Как добавить задание в cron для выполнения каждый день в 2:30? | `30 2 * * * /path/to/command` в crontab (`crontab -e`) |
| 38. Что такое `systemd` и как управлять сервисами? | Современный init system и менеджер сервисов. Команды: `systemctl start|stop|restart|status service_name` |
| 39. Как посмотреть последние 20 строк лог-файла в реальном времени? | `tail -f -n 20 /var/log/file.log` или `journalctl -f -n 20` для systemd |
| 40. Как найти все SUID/SGID файлы в системе? | `find / -perm /6000 2>/dev/null` |

## 🌐 Сетевые технологии

| Вопрос | Краткий ответ |
| :--- | :--- |
| 41. Что такое VXLAN и чем отличается от VLAN? | **VXLAN** - overlay сеть поверх IP (инкапсуляция), до 16M сегментов. **VLAN** - изоляция на уровне L2, только 4094 VLAN. |
| 42. Как настроить статический маршрут в Linux? | `ip route add <network> via <gateway>` или через `/etc/network/interfaces`/NetworkManager |
| 43. Что такое eBPF и для чего используется? | Extended BPF - виртуальная машина в ядре для безопасного выполнения программ в пространстве ядра. Для мониторинга, трассировки, сетевых фильтров. |
| 44. Как настроить bonding/teaming сетевых интерфейсов? | Через NetworkManager (`nmcli`) или конфиги в `/etc/network/interfaces`. Режимы: active-backup, balance-rr, 802.3ad. |
| 45. Что такое nftables и чем заменил iptables? | Современная подсистема фильтрации пакетов, заменила iptables/ip6tables/arptables/ebtables. Единый синтаксис, лучшая производительность. |

## 🐳 Контейнеризация и оркестрация

| Вопрос | Краткий ответ |
| :--- | :--- |
| 46. Как ограничить память для Docker контейнера? | `docker run --memory=512m image_name` или в docker-compose: `mem_limit: 512m` |
| 47. Что такое Kubernetes pod? | Наименьшая deployable единица в K8s - группа из одного или нескольких контейнеров с общим storage/network. |
| 48. Как посмотреть логи контейнера в Docker? | `docker logs <container_id>` или `docker logs -f` для follow |
| 49. В чем разница между ENTRYPOINT и CMD в Dockerfile? | **ENTRYPOINT** - исполняемая команда, **CMD** - аргументы по умолчанию. Можно комбинировать. |
| 50. Как пробросить переменные окружения в Docker? | `docker run -e VAR=value image` или через `--env-file` |

## 🔒 Безопасность

| Вопрос | Краткий ответ |
| :--- | :--- |
| 51. Что такое AppArmor и как работает? | Система mandatory access control (MAC). Профили определяют, к каким ресурсам имеет доступ приложение. |
| 52. Как настроить SSH tunneling? | Локальный: `ssh -L local_port:remote_host:remote_port user@jump_host`. Удаленный: `ssh -R remote_port:local_host:local_port user@remote_host` |
| 53. Что такое mTLS (mutual TLS) и как настроить? | Двусторонняя аутентификация - клиент и сервер проверяют сертификаты друг друга. Настраивается в web-сервере (nginx, apache). |
| 54. Как настроить firewall с nftables? | ```nft add table inet filter<br>nft add chain inet filter input { type filter hook input priority 0 \; }<br>nft add rule inet filter input tcp dport 22 accept``` |
| 55. Что такое seccomp и для чего используется? | Secure computing mode - фильтр системных вызовов, ограничивает доступные syscall для процессов. |

## 💾 Файловые системы и диски

| Вопрос | Краткий ответ |
| :--- | :--- |
| 56. В чем разница между ext4 и XFS? | **ext4** - стабильность, журналирование. **XFS** - высокая производительность с большими файлами, онлайн-расширение. |
| 57. Как восстановить удаленный файл в Linux? | Через `extundelete` (для ext3/4) или `testdisk`. Требуется немедленное действие - не записывать на раздел. |
| 58. Что такое OverlayFS и как используется? | Union filesystem - объединяет несколько директорий (lower+upper). Основа storage драйверов в Docker. |
| 59. Как проверить целостность файловой системы? | `fsck /dev/sdX` (предварительно размонтировать) или `xfs_repair` для XFS. |
| 60. Что такое LUKS encryption и как настроить? | Linux Unified Key Setup - прозрачное шифрование дисков. Настройка: `cryptsetup luksFormat /dev/sdX`, `cryptsetup open /dev/sdX name`. |

## ⚡ Производительность и мониторинг

| Вопрос | Краткий ответ |
| :--- | :--- |
| 61. Как анализировать производительность дисковых операций? | `iostat -x 1`, `iotop`, `fio` для тестирования, `blktrace` для глубокого анализа. |
| 62. Что такое Memory Pressure и как отслеживать? | Нагрузка на подсистему памяти. `ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem` или `cat /proc/pressure/memory` |
| 63. Как настроить huge pages в Linux? | `echo 1024 > /proc/sys/vm/nr_hugepages` или через `sysctl vm.nr_hugepages=1024` |
| 64. Что такое swappiness и как настроить? | Параметр ядра (0-100), определяющий склонность к использованию swap. `sysctl vm.swappiness=10` |
| 65. Как отладить проблему с сетевой производительностью? | `iperf3` для тестирования пропускной способности, `netstat -i` для ошибок, `ethtool` для статистики интерфейса. |

## 🔧 Системное администрирование (продолжение)

| Вопрос | Краткий ответ |
| :--- | :--- |
| 66. Как настроить NTP-клиент для синхронизации времени? | `timedatectl set-ntp true` (systemd) или через `/etc/chrony.conf`, `/etc/ntp.conf` |
| 67. Что такое Tmpfiles.d и для чего используется? | Механизм systemd для управления временными файлами и директориями. Конфиги в `/etc/tmpfiles.d/*.conf` |
| 68. Как создать RAM-диск (tmpfs) в Linux? | `mount -t tmpfs -o size=1G tmpfs /mnt/ramdisk` или через `/etc/fstab`: `tmpfs /mnt/ramdisk tmpfs size=1G 0 0` |
| 69. Что делает команда `echo 3 > /proc/sys/vm/drop_caches`? | Освобождает кэш памяти: 1 - pagecache, 2 - dentries/inodes, 3 - всё вышеперечисленное |
| 70. Как настроить ulimit для пользователя? | Через `/etc/security/limits.conf` или `systemd` директивы `LimitNOFILE`, `LimitNPROC` |

## 🐳 Контейнеризация и Docker

| Вопрос | Краткий ответ |
| :--- | :--- |
| 71. В чем разница между `docker exec` и `docker attach`? | `exec` - выполняет новую команду в работающем контейнере. `attach` - подключается к основному процессу контейнера |
| 72. Как очистить все неиспользуемые Docker-ресурсы? | `docker system prune -a` - удаляет контейнеры, образы, сети, билд-кэш |
| 73. Что такое Docker layer и как оптимизировать образы? | Каждая инструкция в Dockerfile создает слой. Оптимизация: мульти-стадж билды, объединение RUN команд, .dockerignore |
| 74. Как настроить healthcheck в Docker? | В Dockerfile: `HEALTHCHECK --interval=30s CMD curl -f http://localhost/ || exit 1` |
| 75. Что такое Docker buildkit и какие преимущества? | Новая система сборки образов: параллельная сборка, кэширование, секреты, улучшенная производительность |

## ☸️ Kubernetes

| Вопрос | Краткий ответ |
| :--- | :--- |
| 76. Как посмотреть логи пода в Kubernetes? | `kubectl logs <pod-name>` или `kubectl logs -f <pod-name>` для потокового вывода |
| 77. Что такое ConfigMap и Secret в Kubernetes? | **ConfigMap** - хранение конфигурационных данных. **Secret** - хранение чувствительных данных (base64 encoded) |
| 78. Как выполнить команду в работающем pod? | `kubectl exec -it <pod-name> -- /bin/bash` или нужная команда |
| 79. Что такое Ingress Controller и для чего используется? | Прокси-сервер (nginx, traefik) для маршрутизации HTTP-трафика в кластере на основе правил Ingress |
| 80. Как масштабировать deployment? | `kubectl scale deployment/<name> --replicas=5` |

## 🔒 Безопасность (продолжение)

| Вопрос | Краткий ответ |
| :--- | :--- |
| 81. Что такое Falco и для чего используется? | Runtime security tool для обнаружения аномальной активности в контейнерах и хостах на основе правил |
| 82. Как настроить auditd для аудита системных событий? | Через `/etc/audit/audit.rules`: `-w /etc/passwd -p wa -k identity_access` |
| 83. Что такое CIS benchmarks и как применять? | Security best practices для ОС и софта. Инструменты: `lynis`, `OpenSCAP` для автоматической проверки |
| 84. Как настроить PAM (Pluggable Authentication Modules)? | Конфиги в `/etc/pam.d/`. Модули: `pam_tally2` для блокировки при неудачных входах, `pam_limits` для ограничений |
| 85. Что такое certificate pinning и как реализовать? | Фиксация ожидаемого сертификата/публичного ключа. Реализация: через HTTP-заголовки или на уровне приложения |

## 🌐 Сети (продолжение)

| Вопрос | Краткий ответ |
| :--- | :--- |
| 86. Как настроить IPv6 в Linux? | `ip -6 addr add 2001:db8::1/64 dev eth0` или через NetworkManager/systemd-networkd |
| 87. Что такое Anycast и как настроить? | Маршрутизация к "ближайшему" узлу. Настройка: BGP анонс одного префикса из нескольких локаций |
| 88. Как работает DNS resolution в Linux? | Через `/etc/nsswitch.conf` (порядок: files, dns) и `/etc/resolv.conf` (DNS серверы) |
| 89. Что такое WireGuard и как настроить? | Современный простой VPN. Настройка: `wg-quick up wg0`, конфиг в `/etc/wireguard/wg0.conf` |
| 90. Как мониторить сетевой трафик? | `tcpdump`, `tshark`, `iftop`, `nethogs`. Для постоянного мониторинга - Prometheus + node_exporter |

## ⚡ Производительность и мониторинг (продолжение)

| Вопрос | Краткий ответ |
| :--- | :--- |
| 91. Что такое BPF Compiler Collection (BCC)? | Набор инструментов для анализа производительности и сетевого трафика на основе eBPF |
| 92. Как использовать perf для профилирования? | `perf record -g command`, `perf report` для анализа. `perf stat` для общей статистики |
| 93. Что такое USE Method для анализа производительности? | Utilization, Saturation, Errors - методология анализа ресурсов (CPU, memory, disk, network) |
| 94. Как настроить мониторинг с Prometheus? | Установка prometheus, настройка exporters (node_exporter), конфигурация scrape targets |
| 95. Что такое OpenTelemetry и как использовать? | Vendor-neutral framework для сбора телеметрии (metrics, logs, traces). Инструментирование приложений |

## 🛠 DevOps инструменты

| Вопрос | Краткий ответ |
| :--- | :--- |
| 96. Как настроить CI/CD pipeline в GitLab? | Через `.gitlab-ci.yml` в репозитории. Этапы: build, test, deploy. Использование runners |
| 97. Что такое Terraform state и как управлять? | Хранит состояние инфраструктуры. Бэкенды: local, S3, GCS, Azure Storage. Использование state locking |
| 98. Как настроить Ansible dynamic inventory? | Скрипт/программа, возвращающая JSON с хостами. Для cloud провайдеров - `ec2.py`, `azure_rm.py` |
| 99. Что такое GitOps и как реализовать? | Инфраструктура как код + pull-based деплой. Инструменты: ArgoCD, FluxCD |
| 100. Как настроить мониторинг логов в ELK/EFK stack? | **Filebeat** → **Logstash/Fluentd** → **Elasticsearch** → **Kibana**. Настройка парсинга, индексов, дашбордов |

## 💡 Дополнительные важные темы

| Вопрос | Краткий ответ |
| :--- | :--- |
| 101. Что такое service mesh (Istio, Linkerd)? | Слой инфраструктуры для управления service-to-service коммуникацией (traffic management, security, observability) |
| 102. Как настроить backup стратегию? | Инструменты: `borgbackup`, `restic`, `rsnapshot`. Стратегия: 3-2-1 правило (3 копии, 2 носителя, 1 оффсайт) |
| 103. Что такое immutable infrastructure? | Подход, где серверы не модифицируются после деплоя, а заменяются новыми при изменениях |
| 104. Как реализовать zero-downtime деплой? | Blue-green deployment, canary releases, rolling updates с proper health checks |
| 105. Что такое chaos engineering и инструменты? | Тестирование устойчивости системы через внесение сбоев. Инструменты: Chaos Mesh, Litmus, Gremlin |
Отлично! Вот финальные 30 вопросов, охватывающих современные DevOps-практики и продвинутые темы.

## 🔧 Продвинутое системное администрирование

| Вопрос | Краткий ответ |
| :--- | :--- |
| 106. Как настроить Kubernetes cluster с kubeadm? | `kubeadm init --pod-network-cidr=...`, установка CNI, join worker nodes с токеном |
| 107. Что такое CoreDNS в Kubernetes и как настроить? | DNS-сервер для кластера. Конфиг через ConfigMap: `kubectl edit configmap coredns -n kube-system` |
| 108. Как настроить HPA (Horizontal Pod Autoscaler)? | `kubectl autoscale deployment <name> --cpu-percent=50 --min=1 --max=10` |
| 109. Что такое Kustomize и чем отличается от Helm? | **Kustomize** - template-free overlay система. **Helm** - менеджер пакетов с шаблонами. Оба для управления Kubernetes манифестами |
| 110. Как настроить network policies в Kubernetes? | Ресурс NetworkPolicy для контроля трафика между подами. Например, разрешить ingress только из определенных namespace |

## 🛡 Безопасность контейнеров

| Вопрос | Краткий ответ |
| :--- | :--- |
| 111. Что такое OPA/Gatekeeper в Kubernetes? | Open Policy Agent - валидация и enforcement политик безопасности в K8s через admission controllers |
| 112. Как настроить Pod Security Standards? | Через PodSecurity admission: `kubectl label namespace default pod-security.kubernetes.io/enforce=baseline` |
| 113. Что такое distroless образы и преимущества? | Минимальные образы без shell/package manager. Уменьшают attack surface и размер |
| 114. Как сканировать образы на уязвимости? | Инструменты: `trivy`, `grype`, `snyk`. Интеграция в CI/CD pipeline |
| 115. Что такое seccomp профили для Docker/K8s? | Ограничение системных вызовов. Настройка через `securityContext.seccompProfile` в Pod spec |

## ☁ Cloud Native технологии

| Вопрос | Краткий ответ |
| :--- | :--- |
| 116. Как настроить AWS EKS cluster? | `eksctl create cluster` или через Terraform `aws_eks_cluster` resource |
| 117. Что такое service discovery в cloud? | Механизм автоматического обнаружения сервисов (Consul, etcd, cloud-native решения) |
| 118. Как настроить auto-scaling group в AWS? | Через Terraform: `aws_autoscaling_group` с launch template и scaling policies |
| 119. Что такое cloud-init и для чего используется? | Скрипт инициализации cloud instances. Настройка пользователей, пакетов, конфигов при старте |
| 120. Как мигрировать on-premise в cloud? | Стратегия: rehost (lift&shift), replatform, refactor. Инструменты: AWS MGN, Azure Migrate |

## 🔄 Git и CI/CD продвинутые

| Вопрос | Краткий ответ |
| :--- | :--- |
| 121. Что такое Git sparse checkout? | `git sparse-checkout init --cone` - загрузка только части репозитория для больших mono-repo |
| 122. Как настроить GitLab runners autoscaling? | Docker machine executor или Kubernetes executor с auto-scaling на основе pending jobs |
| 123. Что такое ArgoCD и как настроить? | Declarative GitOps tool для K8s. Установка: `kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml` |
| 124. Как реализовать canary deployment в K8s? | Через Istio/Service Mesh или Flagger с постепенным перенаправлением трафика |
| 125. Что такое Tekton и преимущества? | Cloud-native CI/CD framework для K8s. Преимущества: native K8s resources, modularity |

## 📊 Мониторинг и observability

| Вопрос | Краткий ответ |
| :--- | :--- |
| 126. Как настроить Grafana Loki для логов? | `helm install loki grafana/loki-stack` - log aggregation система, аналогичная Prometheus для метрик |
| 127. Что такое OpenMetrics стандарт? | Стандарт формата для метрик, расширяющий Prometheus format |
| 128. Как настроить distributed tracing? | Инструменты: Jaeger, Zipkin. Инструментирование приложений через OpenTelemetry |
| 129. Что такое RED метрики? | **R**ate, **E**rrors, **D**uration - ключевые метрики для мониторинга сервисов |
| 130. Как настроить alertmanager в Prometheus? | Конфигурация routes, receivers, inhibition rules в `alertmanager.yml` |

## 🚀 Производительность и оптимизация

| Вопрос | Краткий ответ |
| :--- | :--- |
| 131. Что такое eBPF и применение в мониторинге? | Extended Berkeley Packet Filter - программы в ядре для безопасного мониторинга без модификации кода |
| 132. Как оптимизировать Linux для high performance? | Настройка: sysctl params (net.core.somaxconn, vm.swappiness), CPU governor, I/O scheduler |
| 133. Что такое memory compaction и fragmentation? | Проблемы управления памятью. Решения: huge pages, defragmentation tools |
| 134. Как настроить CPU pinning в K8s? | Через `cpuManagerPolicy: static` в kubelet и `requests.cpu` в Pod spec |
| 135. Что такое irqbalance и для чего? | Демон для балансировки прерываний между CPU ядрами. Критично для сетевой производительности |

## 🔧 Инфраструктура как код

| Вопрос | Краткий ответ |
| :--- | :--- |
| 136. Как использовать Terraform workspaces? | `terraform workspace new dev` - изоляция state для разных окружений |
| 137. Что такое Terragrunt и преимущества? | Обертка над Terraform для устранения повторения кода, управления зависимостями |
| 138. Как настроить Terraform remote state? | Бэкенд S3/GCS/Azure с state locking через DynamoDB/Cloud Storage |
| 139. Что такое Crossplane в Kubernetes? | Framework для управления cloud resources через K8s API (Infrastructure as Code) |
| 140. Как мигрировать state в Terraform? | `terraform state mv` для перемещения ресурсов между state файлами |

## 🛠 Продвинутые инструменты

| Вопрос | Краткий ответ |
| :--- | :--- |
| 141. Что такое WebAssembly (WASM) в Kubernetes? | `Krustlet` - запуск WASM workloads вместо containers. Преимущества: безопасность, переносимость |
| 142. Как использовать Buildpacks вместо Dockerfile? | `pack build` - автоматическая сборка образов без написания Dockerfile |
| 143. Что такое Dagger для CI/CD? | Portable CI/CD engine как code. Запуск pipelines в containers, кэширование, параллелизация |
| 144. Как настроить confidential computing? | Технологии: Intel SGX, AMD SEV. Защита данных в процессе выполнения |
| 145. Что такое FinOps и инструменты? | Cloud financial management. Инструменты: Infracost, Kubecost, CloudHealth |

## 🎯 Стратегия и best practices

| Вопрос | Краткий ответ |
| :--- | :--- |
| 146. Как организовать multi-cluster management? | Инструменты: Fleet, Karmada. Единое управление несколькими K8s кластерами |
| 147. Что такое platform engineering? | Создание внутренних developer platforms (IDP) с самообслуживанием для разработчиков |
| 148. Как реализовать policy as code? | Инструменты: OPA, Kyverno. Автоматическое enforcement политик безопасности и compliance |
| 149. Что такое MLOps и отличия от DevOps? | DevOps для ML: управление данными, эксперименты, model versioning, monitoring drift |
| 150. Как построить SRE практику в компании? | SLI/SLO/SLA определение, error budgets, blameless postmortems, automation |
